# Plugin examples boundary

Future example plugins demonstrate the out-of-process IPC contract without becoming supported integrations. They may depend on the plugin SDK only as needed for an example. Do not place production plugins, Rust ABI libraries, or platform-specific core dependencies here.
