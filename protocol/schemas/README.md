# Manifest schemas

These draft JSON Schema 2020-12 contracts describe human-facing
`kubeweft.dev/v1alpha1` manifests. They are declarative input contracts; runtime
code should deserialize accepted manifests into typed internal models.
