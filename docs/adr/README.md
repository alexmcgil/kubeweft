# Architecture Decision Records

This directory will hold Architecture Decision Records (ADRs) when a durable project decision needs context, alternatives, and consequences. Add a new, numbered Markdown record rather than silently revising a decision; supersede an older record with a new one when its decision changes. This directory contains no runtime architecture or implementation dependencies.
