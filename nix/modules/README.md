# Nix module boundary

This is reserved for future optional NixOS or Nix integration modules. Modules may compose Nix-level deployment configuration only after an explicit design; they must not make the unfinished agent a required system service, implement capabilities, or introduce a second core configuration model.
