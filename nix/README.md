# Nix boundary

This directory owns reproducible development-shell and future Nix integration documentation. The repository flake provides development tooling, not application packaging or a NixOS deployment. Nix configuration may provide build tools but must not redefine Kubeweft core architecture or Linux agent behavior.
