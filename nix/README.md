# Nix boundary

This directory owns reproducible development-shell and future Nix integration documentation. Task 7 adds the repository flake; it is for development tooling, not application packaging or a NixOS deployment. Nix configuration may provide build tools but must not redefine Kubeweft core architecture or Linux agent behavior.
